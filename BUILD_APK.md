# בניית קובץ APK להתקנה על אנדרואיד 📦

מדריך צעד‑אחר‑צעד להפקת קובץ `.apk` שאפשר להתקין ישירות על טלפון אנדרואיד.
הבנייה מתבצעת בענן של Expo (**EAS Build**) — לא צריך להתקין Android Studio.

## מה צריך מראש
- חשבון **Expo** חינמי — הרשמה ב‑https://expo.dev/signup
- **Node.js 18+** מותקן על המחשב

## השלבים

```bash
# 1. התקנת התלויות של הפרויקט
npm install

# 2. התקנת כלי ה‑EAS (פעם אחת, גלובלי)
npm install -g eas-cli

# 3. התחברות לחשבון Expo שלך
eas login

# 4. קישור הפרויקט לחשבון (יוצר projectId אוטומטית ב‑app.json)
eas init

# 5. בניית ה‑APK (פרופיל preview שכבר מוגדר ב‑eas.json)
npm run build:apk
#   שקול ל: eas build -p android --profile preview
```

בסיום הבנייה (כ‑10–15 דקות בענן) תקבל **קישור להורדה** של קובץ ה‑APK בטרמינל
ובלוח הבקרה של Expo (https://expo.dev). מורידים אותו לטלפון, פותחים, ומאשרים
התקנה ממקור לא ידוע — וזהו, האפליקציה מותקנת. ✅

## הערות
- **מפתח OpenAI:** כדי שהניתוח יגיע מה‑AI האמיתי (ולא מהמעריך המקומי), הגדר את
  המשתנה לפני הבנייה:
  ```bash
  export EXPO_PUBLIC_OPENAI_API_KEY=sk-...
  npm run build:apk
  ```
  (למוצר אמיתי מומלץ להפעיל את קריאות ה‑LLM דרך שרת קטן משלך ולא לשמור מפתח באפליקציה.)
- **גרסה לחנות Google Play:** השתמש ב‑`npm run build:aab` שמפיק קובץ `.aab`
  (App Bundle) לפי פרופיל `production` ב‑`eas.json`.
- הפרופילים מוגדרים ב‑[`eas.json`](eas.json): `preview` → APK להתקנה ידנית,
  `production` → AAB לחנות.

## למה אי אפשר לבנות את ה‑APK כאן אוטומטית?
בניית APK דורשת התחברות (`eas login`) לחשבון ה‑Expo **שלך** והרצה בשרתי הענן של
Expo תחת החשבון הזה. לכן כל התצורה מוכנה בקוד, והשלבים למעלה הם כל מה שנשאר.
